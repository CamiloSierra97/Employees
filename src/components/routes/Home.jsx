import React from "react";
import FormLogin from "../login/FormLogin";

const Home = () => {
  return (
    <div className="home">
      <main className="home__container-card">
        <FormLogin />
      </main>
    </div>
  );
};

export default Home;
